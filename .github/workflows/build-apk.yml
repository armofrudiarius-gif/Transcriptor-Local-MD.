name: Build Transcriptor Local MD v3.0
on:
  workflow_dispatch:

jobs:
  staging:
    runs-on: ubuntu-latest
    steps:
      - run: echo "v3.0 source staging; build resumes when workflow is finalized."
